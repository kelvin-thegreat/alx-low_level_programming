#include <stdio.h>
/**
 * main -Entry point
 *
 * main: prints out a string to stdout.
 *
 * return: 0 if success.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
